entity: Application
parameters: {}
items:
  application{1..2}:
    id: '{{random.uuid}}'
    name: 'application_($current)'
    description: '<p>{{lorem.paragraph}}</p>'
    createdAt: '{{date.past}}'
    updatedAt: '{{date.recent}}'
    users:
      - '@user($current)'
